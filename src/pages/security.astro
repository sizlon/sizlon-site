---
// 구 크롤러 보안 → 데이터 수집 원칙 (v3, 2026-09-06)
// 손으로 쓴 스텁(2026-08-08 규약): noindex 를 달지 않고 canonical + sitemap 제외로
// 색인에서 뺀다. 진짜 301 은 앞단 Cloudflare 규칙이 낸다(v3 §1) — 이 파일은 폴백.
const target = '/services/data/#principles';
import RedirectStub from '../components/RedirectStub.astro';
---
<RedirectStub target={target} />
